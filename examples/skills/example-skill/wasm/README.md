This directory should contain a WASI component named `skill.wasm` that exports:

execute(input_json: string) -> string

For now, `skill.wasm` is a placeholder file. Replace it with a real component
compiled from Rust or TinyGo following the OpenSkill WIT contract.
