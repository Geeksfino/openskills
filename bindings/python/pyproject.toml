[build-system]
requires = ["maturin>=1.4"]
build-backend = "maturin"

[project]
name = "openskills"
version = "0.1.0"
description = "OpenSkill Runtime Python bindings"
license = { text = "MIT" }
requires-python = ">=3.9"

[tool.maturin]
bindings = "pyo3"
