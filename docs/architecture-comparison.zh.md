# OpenSkills 与 Claude Skills 架构对比

## 渐进披露流程对比

### Claude Skills 官方流程

```
会话开始
     │
     ├─► 扫描目录 (~/.claude/skills, .claude/skills/)
     │
     ├─► 加载第1层 (元数据)
     │   ├─ name: "code-review"
     │   └─ description: "Reviews code..."
     │   (~100 tokens per skill)
     │
     ├─► 注入系统提示词 ⭐
     │   "You have access to these skills:
     │    - code-review: Reviews code...
     │    - test-generator: Generates tests..."
     │
     └─► 模型了解可用技能

用户请求: "Review my code"
     │
     ├─► 语义匹配描述
     │   → "code-review" 匹配
     │
     ├─► 加载第2层 (指令) ⭐
     │   → 完整的 SKILL.md markdown 正文
     │   (~2000 tokens)
     │
     └─► 执行技能指令

技能需要脚本/资源
     │
     └─► 加载第3层 (资源) ⭐
         → scripts/review.py (源代码不在上下文中)
         → 仅输出进入上下文
```

### OpenSkills 更新流程

```
runtime.discover_skills()
     │
     ├─► 扫描目录 (~/.claude/skills, .claude/skills/, custom)
     │
     ├─► 加载第1层 (元数据) ✅
     │   ├─ id: "code-review"
     │   └─ description: "Reviews code..."
     │
     └─► 返回 Vec<SkillDescriptor>

✅ 系统提示词助手可用
     │
     └─► let prompt = runtime.get_system_prompt_metadata();

runtime.activate_skill("code-review")
     │
     ├─► 加载第2层 (指令) ✅
     │   → 完整的 SKILL.md 内容
     │
     └─► 返回 LoadedSkill

runtime.execute_skill("code-review", options)
     │
     ├─► 加载第3层 (WASM 模块) ✅
     │   → 查找 wasm/skill.wasm
     │   → 按需加载
     │
     └─► 在 WASM 沙箱中执行
```

---

## 上下文分叉机制

### Claude Skills 带上下文分叉

**重要**: 分叉上下文在技能激活**之后**开始，不是之前。

```
主上下文 (用户对话)
│
├─ 用户: "Explore the authentication system"
│
├─ 步骤 1: 技能激活 (主上下文) ⚠️
│  ├─ Agent 调用: activate_skill("explorer")
│  ├─ 运行时加载完整 SKILL.md 指令
│  ├─ 指令返回到主上下文
│  └─ LLM 在主上下文中读取/理解指令
│
├─ 步骤 2: 分叉创建 (执行阶段) ⭐
│  ├─ Agent 调用: start_skill_session("explorer") 或 execute_skill_with_context()
│  ├─ 运行时检测: manifest 中的 context: fork
│  └─ 分叉上下文在此处创建 (隔离的子 Agent)
│      │
│      └─► 分叉上下文 (隔离执行)
│          ├─ 读取 auth.ts → 在分叉中输出
│          ├─ 读取 user.ts → 在分叉中输出
│          ├─ 分析模式 → 在分叉中输出
│          ├─ 调试输出: "Found 3 auth flows..." → 在分叉中输出
│          ├─ 尝试 1: 检查 JWT → 在分叉中输出
│          ├─ 尝试 2: 检查 OAuth → 在分叉中输出
│          └─ 生成摘要
│              "Authentication uses JWT + OAuth2.
│               3 main flows: login, refresh, logout."
│
├─ 步骤 3: 摘要返回 (主上下文)
│  ├─ ⬅️ 摘要注入到主上下文
│  └─ (不是所有中间步骤)
│
└─ 用户看到干净的摘要，不是调试日志

令牌节省: ~5000 tokens (中间工作保持隔离)
用户体验益处: 干净的对话流
```

**关键要点**:
- **激活在主上下文中进行**: 技能指令在主对话中加载并读取
- **分叉在执行期间开始**: 隔离的上下文在执行开始时创建
- **仅执行输出被分叉**: 工具调用、错误和调试日志保持在分叉中
- **摘要返回主上下文**: 最终结果被提取并返回到主上下文

### OpenSkills 更新行为

```
主上下文 (用户对话)
│
├─ runtime.execute_skill_with_context("explorer", options, &ctx)
│  │
│  └─► 分叉上下文 ✅
│      ├─ 读取 auth.ts → 在分叉中输出
│      ├─ 读取 user.ts → 在分叉中输出
│      ├─ 分析模式 → 在分叉中输出
│      ├─ 调试输出 → 在分叉中输出
│      ├─ 尝试 1 → 在分叉中输出
│      ├─ 尝试 2 → 在分叉中输出
│      └─ 摘要返回到父上下文
│
└─ 用户仅看到摘要

✅ 上下文隔离，减少令牌使用
```

---

## 权限模型对比

### Claude Skills: 先询问后行动

```
技能: "file-organizer"
allowed-tools: Read, Write, Bash

用户: "Organize my downloads folder"

Claude: "I'll use the file-organizer skill."

技能尝试执行: rm -rf old_files/

┌─────────────────────────────────────────────┐
│ ⚠️  需要权限                                 │
│                                             │
│ 技能想要执行:                               │
│   • 工具: Bash                              │
│   • 命令: rm -rf old_files/                 │
│   • 风险: 永久删除文件                      │
│                                             │
│ [允许一次] [始终允许] [拒绝]                │
└─────────────────────────────────────────────┘

用户点击 [允许一次]

✅ 执行继续
📝 审计日志: 用户在 2026-01-18 10:30 批准了 Bash
```

### OpenSkills: 先询问后行动权限

```
技能: "file-organizer"
allowed-tools: Read, Write, Bash

runtime.with_permission_callback(CliPermissionCallback)
       .execute_skill("file-organizer", options)

技能尝试执行: rm -rf old_files/

┌─────────────────────────────────────────────┐
│ ⚠️  需要权限                                 │
│                                             │
│ [允许一次] [始终允许] [拒绝]                │
└─────────────────────────────────────────────┘

✅ 仅在批准后执行继续
📝 审计日志: 记录用户决定
```

---

## 沙箱架构

### Claude Skills: OS级沙箱

```
┌─────────────────────────────────────────┐
│   macOS 进程                            │
│   ┌───────────────────────────────┐     │
│   │ 沙箱 (seatbelt)              │     │
│   │                               │     │
│   │  skill.sh                     │     │
│   │    │                          │     │
│   │    ├─ read: /allowed/path ✅  │     │
│   │    ├─ write: /tmp/out ✅      │     │
│   │    └─ access: /etc/passwd ❌  │     │
│   │       (被 seatbelt 阻止)      │     │
│   └───────────────────────────────┘     │
└─────────────────────────────────────────┘

优点: 本地性能
缺点: OS 特定 (seatbelt/seccomp)
```

### OpenSkills: WASM 沙箱

```
┌─────────────────────────────────────────┐
│   任何 OS (macOS/Linux/Windows)         │
│   ┌───────────────────────────────┐     │
│   │ Wasmtime 运行时               │     │
│   │                               │     │
│   │  skill.wasm (WASI)            │     │
│   │    │                          │     │
│   │    ├─ preopens:               │     │
│   │    │  /skill → 仅读 ✅         │     │
│   │    │  /tmp → 读写 ✅           │     │
│   │    │                          │     │
│   │    └─ access: /etc/passwd ❌  │     │
│   │       (不在 preopens 中)      │     │
│   └───────────────────────────────┘     │
└─────────────────────────────────────────┘

优点: 跨平台、可移植、基于能力
缺点: 需要 WASM 编译
```

---

## 令牌优化对比

### Claude Skills: 强制最佳实践

```
SKILL.md 令牌预算:
├─ 第1层 (元数据)
│  ├─ name: ~2 tokens
│  ├─ description: ~30 tokens (max 1024 chars)
│  └─ 总计: ~100 tokens/skill ✅
│
├─ 第2层 (指令)
│  ├─ Markdown 正文: ~2000 tokens (推荐)
│  ├─ ⚠️ 如果 > 5000 tokens 警告
│  └─ 验证: skills-ref validate
│
└─ 第3层 (资源)
   ├─ scripts/ → 0 tokens (不在上下文中)
   ├─ references/ → 0 tokens (按需加载)
   └─ 仅输出计数 ✅

总初期成本: ~100 tokens
按需成本: 2000-5000 tokens
```

### OpenSkills: 令牌分析可用

```
SKILL.md 令牌预算:
├─ 第1层 (元数据)
│  ├─ 受限: name max 64 chars ✅
│  ├─ 受限: description max 1024 chars ✅
│  └─ 令牌分析通过 `openskills analyze` ✅
│
├─ 第2层 (指令)
│  ├─ 分析输出中的长度估计 ✅
│  ├─ 通过 `openskills validate` 的验证工具 ✅
│  └─ CLI 中的自动化报告 ✅
│
└─ 第3层 (资源)
   ├─ WASM 模块 → 0 tokens ✅
   └─ 按需加载 ✅

✅ 通过分析输出自动化指导
```

---

## 验证工作流

### Claude Skills

```
开发者编写 my-skill/SKILL.md
     │
     ├─► skills-ref validate my-skill
     │   ├─ ✅ 格式有效
     │   ├─ ⚠️ 描述太长 (截断为 1024 chars)
     │   └─ ℹ️ 第2层是 3500 tokens (良好)
     │
     ├─► skills-ref analyze my-skill
     │   ├─ 第1层: 95 tokens
     │   ├─ 第2层: 3500 tokens
     │   ├─ 参考: 2 个文件 (不计数)
     │   └─ 优化分数: A
     │
     └─► CI/CD 管道
         ├─ 运行验证
         ├─ 错误时阻止
         └─ 部署 ✅
```

### OpenSkills

```
开发者编写 my-skill/SKILL.md
     │
     ├─► openskills validate my-skill
     │   ├─ ✅ 格式有效
     │   └─ ⚠️ 警告 (可选)
     │
     ├─► openskills analyze my-skill
     │   ├─ 第1层令牌估计
     │   ├─ 第2层令牌估计
     │   └─ 优化提示
     │
     └─► CI 可对验证进行门控 ✅
```

---

## 数据流摘要

### 渐进披露时间表

```
时间 →

Claude Skills:
   0ms     100ms           5000ms            10000ms
   │       │               │                 │
   ├─ 扫描 ├─ 加载第1层  ├─ 匹配+第2层   ├─ 执行+第3层
   │       │ (所有技能)   │ (1个技能)      │ (按需)
   │       └─► 注入提示词                │
   └─► 会话准备                        └─► 完成
       模型知道技能

OpenSkills:
   0ms     100ms           200ms             5000ms
   │       │               │                 │
   ├─ 扫描 ├─ 加载第1层  ├─ 注入提示词  ├─ 执行+第3层
   │       │ (所有技能)  │ (助手)       │ (按需)
   │       └─► 返回 Vec  └─► 模型感知   └─► 完成
```

---

## 实现检查表

### OpenSkills 做得很好的地方 ✅

- [x] 第1层元数据加载
- [x] 第2层指令加载  
- [x] 第3层资源加载
- [x] SKILL.md 解析 (YAML + Markdown)
- [x] 标准目录发现
- [x] 自定义目录支持
- [x] 沙箱隔离 (WASM)
- [x] 权限映射 (allowed-tools → 能力)
- [x] 审计日志记录
- [x] 格式验证 (名称/描述约束)
- [x] 系统提示词元数据助手
- [x] 先询问后行动权限 (回调)
- [x] 上下文分叉 + 摘要返回
- [x] 验证 CLI + 令牌分析
- [x] 跨平台一致性

### 剩余考虑 (客户端)

- [ ] 当指定 `agent` 时，子 Agent 选择和编排
- [ ] 可选的最佳实践 linting 或 CI 策略强制

---

## 结论

**OpenSkills 现在全面实现了 Claude Skills 架构的运行时范围**，并采用了更优越的跨平台沙箱模型。

**渐进披露机制全面运作** - 所有三个层级按照 Claude Skills 架构中的规定正确加载和按需加载。

**关键区别**: OpenSkills 用便携式 WASM 执行交换 OS 特定的本地执行，这是跨平台部署的战略优势。

---

*图表版本: 1.0*  
*日期: 2026-01-18*
